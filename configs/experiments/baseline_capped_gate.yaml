# Same model and schedule as baseline, with a capped classifier gate grid.
# To retune existing weights use python -m src.tools.retune; this config trains a new run.
extends: ../baseline.yaml
run_name: baseline_capped_gate
eval:
  area_caps: [0.0, 0.008, 0.0095, 0.01]
  cls_thresholds: [0.0, 0.5, 0.6, 0.65, 0.7, 0.74, 0.78, 0.8, 0.82, 0.84, 0.86, 0.9, 0.95]
